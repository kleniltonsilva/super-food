# ============================================
# DEREKH FOOD - Dependências do Sistema
# Versão: 2.0 (SQLAlchemy Multi-Tenant)
# ============================================

# ==================== CORE ====================

# Banco de Dados - SQLAlchemy ORM
sqlalchemy>=2.0.25

# Migrations (para produção)
alembic>=1.13.1

# Variáveis de Ambiente
python-dotenv>=1.0.1

# ==================== BACKEND API ====================

# FastAPI + Uvicorn
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Autenticação JWT
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # passlib 1.7.4 incompatível com bcrypt>=4.1 (ValueError em detect_wrap_bug)

# Validação
pydantic[email]>=2.10.3

# Upload de arquivos
python-multipart>=0.0.20

# Processamento de imagens (resize, crop, WebP)
Pillow>=10.0.0

# Templates (Jinja2 para Starlette)
jinja2>=3.1.0

# WebSockets (realtime)
websockets>=14.1

# ==================== INTEGRAÇÕES ====================

# HTTP Client (Mapbox, APIs externas)
requests>=2.32.3

# Async HTTP Client (integrações marketplace: iFood, Open Delivery)
httpx>=0.27.0

# Cálculo de Distâncias
haversine>=2.8.1

# ==================== PRODUÇÃO ====================

# PostgreSQL Driver
psycopg2-binary>=2.9.9

# Redis (cache, rate limiting, WebSocket pub/sub)
redis>=5.0.0

# Storage S3/R2 (Cloudflare R2)
boto3>=1.34.0

# Monitoramento de erros (Sentry)
sentry-sdk[fastapi]>=2.0.0

# Logging JSON (producao)
python-json-logger>=2.0.7

# Gunicorn (WSGI/ASGI server producao)
gunicorn>=21.2.0

# ==================== SEED / TESTES ====================

# Dados ficticios para seed de teste
Faker>=22.0.0

# ==================== DESENVOLVIMENTO (OPCIONAL) ====================

# pytest>=8.3.4
# black>=24.10.0
# flake8>=7.1.1
# mypy>=1.7.0